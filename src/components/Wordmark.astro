---
---
<div class="wordmark">parametre<span class="wordmark__dot">:</span>art</div>

<style>
  .wordmark {
    font-family: var(--font-display);
    font-size: var(--text-wordmark);
    font-weight: 700;
    letter-spacing: -0.025em;
    line-height: 1.1;
    color: var(--color-text);
  }
  .wordmark__dot {
    color: var(--color-accent);
  }
</style>
