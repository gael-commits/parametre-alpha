{
  "name": "alpha",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node inject-ga4.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=parametre-alpha --branch=main --commit-dirty=true"
  },
  "dependencies": {
    "astro": "^5.17.1",
    "gsap": "^3.15.0"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "prettier-plugin-astro": "^0.14.1"
  }
}
